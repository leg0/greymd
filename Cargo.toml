[package]
name = "greymd"
version = "1.5.0"
edition = "2024"

[features]
math = ["dep:latex2mathml"]

[dependencies]
latex2mathml = { version = "0.2", optional = true }

[build-dependencies]
miniz_oxide = "0.8"
